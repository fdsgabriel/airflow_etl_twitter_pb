sudo apt-get update
sudo apt install python3-pip
sudo pip install apache-airflow
sudo pip install pandas
sudo pip install s3fs
sudo pip install tweepy
sudo pip install pandasql

#Airflow credecials
username: admin  password: fAt992ZEKCdhVwWP